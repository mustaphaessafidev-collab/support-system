backend project install
initialize node project
1 npm init -y
install packages
2 npm install express dotenv cors mongoose jsonwebtoken bcryptjs socket.io
3 npm install -D nodemon
4 npm install cors
5 pour run file admin in data bes backend "node src/seed/adminSeed.js"

cont admin password "admin123" email "admin@gmail.com"  